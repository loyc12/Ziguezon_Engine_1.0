==== Economy Integration ====

HIGH PRIORITY ( Required for basic functionality ) :

1. Define inter-Econ transfer abstraction
  - Allow teleporting resources and infra from one econ to another ( for now )
  - Needed to boostarp econs where none exist prior

2. Infrastructure maintenance resource sinks
  - Per-inf maintenance costs
  - Soft shutdown when unpaid (activity ratio scaling)
  - Hard decay when unpaid long-term (optional)

3. Add collapse revert mechanism
  - smart industry/infrastructure decay


MEDIUM PRIORITY :

1. Implement true resource transport between econs
2. Implement more efficiency modifiers ( like sunAccess )


LOW PRIORITY :

1. Add fractional carry buffers for res production ( avoid rounding losses )
2. Implement local res distribution system ( POWER_GRID, etc )
3. Add tax / revenue mechanics for COMMERCE and auto-trade
4. Building management automation ( add and del )
